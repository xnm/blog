language: node_js
cache:
  directories:
  - node_modules
node_js:
  - "6"
script:
  - npm run build
deploy:
  - provider: pages
    skip_cleanup: true
    github_token: $GITHUB_TOKEN
    local_dir: dist
    target_branch: master
    on:
      branch: src
