/**
 * @description the markdown sources configuration
 * */
export interface SourceConfig {
  /** directory to lookup posts markdown files*/
  posts: string;

  /** directory to lookup pages markdown files*/
  pages: string;
}
