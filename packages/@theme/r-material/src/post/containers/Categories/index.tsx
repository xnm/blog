import * as React from 'react';

type CategoriesProps = BlogApiModel.CategoriesOverview;

const Categories: React.ComponentType<CategoriesProps> = (props: CategoriesProps) => {
  return <div>Categories</div>;
};

export default Categories;
