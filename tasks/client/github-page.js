/** Created by Aquariuslt on 08-15-2016.*/
/**
 * tasks to force push dest folder to github pages
 * require you local have github ssh keys.
 * */

var config = require('../config/config');
var logger = config.logger;

var gulp = require('gulp');